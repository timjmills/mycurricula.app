// The Subject view — year-long single-subject curriculum overview
// (planning_document §5.6). Renders the SubjectView component which
// reads/writes `subjectView` from shared planner state.
import { SubjectView } from "@/components/subject";

export default function SubjectPage() {
  return <SubjectView />;
}
