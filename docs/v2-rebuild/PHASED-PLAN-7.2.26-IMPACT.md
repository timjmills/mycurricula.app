No Phase 0/1/2/3/4 BOUNDARY moves — every phase keeps its gate; the 7.2.26 deltas land as scope changes inside existing phases. Phase 1 (this wave) grows: W3.3b (chrome.css 7.2 delta port), W3.8b (Day edit), W3.8c (Week edit) are added; W3.8 is redefined from the "8-tab document" to the shared lesson editor + Esc/Exit-only modal; W3.4 additionally creates a `/planner` stub route (net-new — the route does not exist on this branch despite CLAUDE.md's status table). Phase 2 REORDERS internally: Planning Hub builds FIRST among its parallel workstreams (it is now the Plan nav destination and the intended immersive Plan surface — a recorded divergence from the bundle, whose Home console still opens the legacy PlanPage; flag to design). Phase 2 data-model migrations add CustomWall{anchor:'lesson', lessonId} (WallBackground is already listed — refine it to per-wall keying incl. custom walls); the Resource Wall workstream adds solo-board edit bar, lesson-anchored promotion, section lesson tags (3 + "+N"), hub linked-wall chip, per-wall backgrounds with frosted plate/scrim, card default med→icon, board→"custom wall" naming, plus immersive-shell enrollment for /post. Phase 2 notifications/To-Do/tools-dock scope is CONFIRMED, not grown — the changelog §6 items are pre-existing 6.24 features (tools-dock.jsx byte-identical), so no new porting work; same for the Planner-Hub header recolor (annotation only). Phase 3 Teach adds immersive enrollment + the Teach-only 2.8s auto-hide bar. Persistence fidelity unchanged: localStorage-grade now, Supabase realtime stays the Phase 1B/4 gate.