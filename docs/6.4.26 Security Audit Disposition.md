# Security Audit — Disposition (2026-06-04)

> Codex security audit of `master` (audited at `b1e7fe7`). 25 findings.
> Remediated via an ultraplan + agent-team pass on branch
> `claude/friendly-ride-zExL7`. Codex gate is owner-run (CLI absent in the
> cloud container — see `docs/review-gate-cloud.md`); each fix carries a
> written self-review + tsc/lint/build. **A re-run of the Codex gate on the
> remediated HEAD is recommended before relying on these fixes.**

## Status legend
- ✅ **Fixed this pass** · 🟢 **Already fixed** (audit predated it) ·
  🟡 **Partial** (tractable part fixed, rest tracked) · ⏸️ **Deferred**
  (deep flip-blocker or owner/infra action)

## Critical

| # | Finding | Status | Notes |
| --- | --- | --- | --- |
| 1 | Committed Claude bypass token (public repo) | ✅ + 🔴 owner | Removed from HEAD (probe script + doc). **Token still in git history — owner MUST rotate the Cloudflare `CLAUDE_BYPASS_TOKEN` now + consider a history purge.** |
| 2 | Users can self-assign `teachers.school_id` | ✅ | Migration `…140000`: dropped `teachers_insert`; column-GRANT limits self-update to profile cols only. |
| 3 | Provisioning enrolls anyone into the oldest school | ✅ + owner | `ensureTeacherRecord` fail-closed on an email-domain allow-list (`ALLOWED_TEACHER_EMAIL_DOMAINS`). **Owner must set the env or nobody provisions.** |

## High

| # | Finding | Status | Notes |
| --- | --- | --- | --- |
| 4 | `can_read_grade` type-invalid SQL | 🟢 | Fixed last round (`IN (SELECT auth_teacher_grade_ids())`). |
| 5 | Cross-school assignment / stale grade | ✅ | `tga_write` pins to caller's school; grade trigger derives from unit (last round). |
| 6 | Author-trusting team-write RLS | ✅ | Split personal/team policies on day_events/extra_events/todos. |
| 7 | Shared lesson sections editable w/ read-only grade | ✅ | Team/master section writes require `can_edit_subject_master`; parent-validation trigger. |
| 8 | Forgeable audit records | ✅ | Dropped direct insert policy; RPC stamps actor + rejects unreadable scope. |
| 9 | Stored HTML XSS (`dangerouslySetInnerHTML`) | ✅ | `lib/sanitize-html.ts` (isomorphic-dompurify allowlist) at every sink + on write. |
| 10 | R2 uploads/ownership client-controlled | 🟡 | RLS identity-immutability trigger added (migration). **API-route hardening (server-bound pending uploads, object verification, MIME/size enforcement) DEFERRED — tracked below.** |
| 11 | `/api/og-preview` SSRF + unbounded body | ✅ | DNS-resolved private/loopback/link-local/metadata blocklist, per-redirect re-check, 512KB stream cap, timeout, text/html-only. |
| 12 | Preview deploys share prod state | ⏸️ owner | `wrangler.preview.jsonc` binds prod worker/R2. Needs isolated preview Supabase/worker/bucket creds — owner/infra. |
| 13 | Hydration retains another user's doc | 🟢 | Fixed last round (reset to empty on owner-change/empty/error under the flag). |
| 14 | Master mode / "Save to Core" doesn't write shared curriculum | ⏸️ | Forking-model master-write persistence is Phase 1B; today it forks/no-ops. Needs an authorized master write scope. |
| 15 | Planner persistence data-loss paths | 🟢 | Fixed last round (week-0 move, blank dup, authored edits, section delete-before-insert). |
| 16 | Configured school week ignored | 🟡 | Source mapping fixed last round; **client views (WeeklyGrid/DailyView/weekly-board) still assume fixture day order — DEFERRED.** |
| 17 | Live views depend on Grade-5 fixture catalogs | ⏸️ | Pre-flip blocker — route real grade-scoped subjects/units/standards through the store (not mock `ALL_UNITS`). |
| 18 | Teach Supabase mode sends mock IDs / loses edits | ⏸️ | Teach-side identity + persistence flip — same class as the planner identity work, not yet done. |
| 19 | Local state leaks across accounts (shared browser) | ✅ | Per-uid localStorage namespacing + clear-on-login/switch (teach-groups, board-annotations, catchup incl. free-text notes). |
| 20 | Importer seeds named-student notes into shared master | ✅ | 4 named notes scrubbed from `lessons.ts`/`_planner-data.sql`; importer value-scan guard (`assertNoPersonalNotes`). |

## Medium

| # | Finding | Status | Notes |
| --- | --- | --- | --- |
| 21 | Open redirect in auth/bypass | ✅ | Shared `safeRelativePath` (same-origin relative only; catches `//`, `\`, control chars, post-resolution `//evil`). |
| 22 | UI reports actions that don't persist | ⏸️ | Mock-mode honesty (catch-up/onboarding/resource-composer) — resolves with the persistence flip. |
| 23 | Tooltip/touch/a11y gaps | 🟡 | Touch targets fixed last round; canonical Tooltip/Button a11y refinements DEFERRED. |
| 24 | Prod CSP includes `unsafe-eval` | ✅ | Gated to dev-only; prod `script-src` drops it. |
| 25 | No automated test gate (auth/RLS/planner/uploads/Teach/week) | ⏸️ | Recommend a CI job (vitest/playwright + a Supabase RLS test harness) wired into `deploy.yml`. |

## Owner action items (cannot be done from the cloud container)
1. **Rotate `CLAUDE_BYPASS_TOKEN`** (Cloudflare secret) — the old value is in git history. Optionally purge history (BFG / `git filter-repo`) + force-push.
2. **Set `ALLOWED_TEACHER_EMAIL_DOMAINS`** (Cloudflare secret + `.env.local`) — comma-separated `domain` or `domain:school_id`; without it, provisioning fails closed (no one gets access).
3. **Apply migrations** `20260604120000` + `20260604140000` on staging, smoke-test RLS (a 2nd teacher can't read tenant-1 data; can't self-change school), then prod.
4. **Isolate preview deploy resources** (#12) before using preview branches against real data.
5. **Re-run the Codex gate** on the remediated HEAD.

## Tracked engineering follow-ups (next waves)
- #10 R2 upload-route hardening · ~~#16 client-side configured-week contract~~ (done) ·
  #17 real catalog hydration · #18 Teach identity/persistence flip ·
  #14 authorized master-write · #25 automated security test gate · #22 mock-mode
  action honesty · #23 Tooltip/Button a11y.

## Addendum — Codex re-audit round 2 (2026-06-04, against `847c449`)

A second Codex pass after the first remediation surfaced 6 more items + drove
the deferred #16. All resolved this wave (commits `ab9fb11`, `1ce6921`,
`ae1819d`, `65c347d`):

| Codex# | Finding | Fix |
| --- | --- | --- |
| 1 | audit RPC forges arbitrary `school_id`/grade | `log_audit_event` now also validates `p_school_id = auth_teacher_school_id()` + grade-belongs-to-school (migration `…150000`). |
| 2 | `master_events_write` subject-only → guessed unit_id derives a foreign grade | BEFORE trigger `validate_master_event_unit` rejects unit_id not belonging to subject_id. |
| 3 | hydration clears stale data after paint (one-frame flash) | Empty INITIAL doc under the flag + additive `hydration` status + owner-keyed readiness — never paints mock/prior-user data. |
| 4 | `getSectionsBatch` unchunked `.in()` | Chunked through `chunkedIn` (150). |
| 5 | `setSections` non-atomic replace | Transactional `replace_lesson_sections` RPC (SECURITY INVOKER). |
| 6 | instance-labels global localStorage keys | Namespaced by auth uid (personal + team); anon→clear-on-login; drop on switch. |
| (audit #16) | configured school week ignored by views | WeeklyGrid/DailyView/weekly-board route through `useOrderedWeekdays` (one ordered-week contract); CSS column count `var(--day-count)`. Sun–Thu default byte-identical. |

**Process note:** five parallel agents each running `git stash` for isolated
verification caused stash collisions (one agent's fix was left stranded in a
stash, recovered via `git checkout stash@{0} -- <file>`). Future multi-agent
waves: instruct agents NOT to use `git stash`; the lead commits each file
path-limited as agents report, and never runs git surgery while agents are
mid-edit.

**Still open:** #17 catalog hydration (the largest pre-flip blocker) is the
next dedicated pass; the round-1 owner action items (token rotation,
provisioning env, migration apply, preview isolation, Codex re-run) still stand,
now including migration `20260604150000`.
