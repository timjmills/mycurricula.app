# Unified v2 Plan — mycurricula.app (2026-07-23)

> **This is the canonical go-forward plan.** It reconciles the sequential design
> handoffs (6.12 → 6.24 → 7.2 → 7.21) with what is actually shipped on `master`, and
> supersedes the scattered/partly-stale plan docs listed in §7. When any older plan doc
> disagrees with this file about current state or direction, **this file wins.**
>
> Verified against `origin/master @ a6466bb` (2026-07-23); **v1.1 re-verified against
> `79b9161`** (same day, after #67/#68 landed). Companion contracts that
> remain authoritative and are NOT superseded: `CLAUDE.md` (policy/phasing),
> `BUILD_STANDARD.md` (visual contract), `AGENTS.md` (Codex-side gate).
>
> **v1.1 (2026-07-23) — audit refresh.** A 3-agent audit verified every §1/§5 repo claim
> against `origin/master` and amended: drift from #67 (sharing-management UI) + #68
> (/welcome pricing) folded in; §2 frame/theme **vocabulary map** added (handoff names ≠
> shipped tokens); the **Refine tab, timeline authoring, and Needs-Attention drawer** now
> owned in §5.2; `CLAUDE.md` §1 staleness resolved (contradiction 8); §7 doc-map corrected.

---

## 1. Current reality (read this first)

**`master` is v2. v2 is LIVE in production, default-ON.** The July cutover was executed,
briefly reverted (a measurement artifact + a real hydrate-honesty gap, both since fixed),
and **re-cut + shipped** — prod runs v2. `lib/v2-flag.ts`: `V2 = process.env.NEXT_PUBLIC_V2 !== "0"`
(default ON, router-gated); the deploy workflow sets `NEXT_PUBLIC_V2=1`.

Any statement elsewhere that "master is v1 / the cutover was rolled back / a push freeze
is in effect" is **stale** — it described a real intermediate state that no longer holds.

### Shipped & live on master
- All 10 `-v2` component families, router-wired: `day-v2`, `week-v2`, `year-v2`,
  `lesson-plan-v2`, `planner-v2`, `hub-v2` (`/planner`), `resource-wall-v2` (`/post`,
  net-new), `catchup-v2`, `teach-v2`, `onboarding-v2`; plus `components/chrome` +
  `components/shell` (the v2 chrome + router gate).
- Router gate (flag-OFF = genuine v1 rollback) on `/daily`, `/weekly`, `/year`, `/home`,
  `/catch-up`; `/planner` + `/post` net-new. `/subject` retired → redirect to `/year`.
- Waves 1–12 landed, incl. **W11 Teach**, **W12a settings re-skin**, **W12b-1
  course-sharing seam** (`lib/subjects/*` + migration `20260717120000_course_sharing_rpcs.sql`),
  **W12c onboarding-v2** (workspace-first wizard).
- Post-cutover honesty fixes: loading-states (`usePlannerDataState` + `PlannerEmpty` +
  `Skeleton`) and grade-resolution-failure-now-errors.
- *(v1.1)* **Settings sharing-management UI** — #67 `73a97ca` (U-SHARE): a Course-sharing
  card on Settings › Subjects (`components/settings/course-sharing-manager.tsx`, wired to
  `lib/subjects/client`). `components/settings/` is no longer empty (course-sharing-manager,
  workspace-settings, save-confirm-dialog) — but there is still no ConfigPage (see below).
- *(v1.1)* **/welcome pricing honesty** — #68 `79b9161`: School tier + "$6/teacher" removed;
  Free + Basic $2.99 + "AI coming soon" (closes the §6 business-model cleanup item).

### NOT built yet (the remaining work — see §5)
- The **7.21 Unified Unit/Lesson Workspace** (`ph-workspace`), the **Shared Composer**,
  the pop-in unit overlay, and the `--uc` vibrant recolor.
- The **full Settings/Setup ConfigPage** — only an in-place re-skin of
  `app/settings/layout.tsx` shipped (W12a); there is no `components/settings-v2`
  ConfigPage yet.
- W12 tail: **12b-2 multi-workspace** (no pushed branch as of v1.1 — if a session holds
  in-flight work it is local-only; log a claim before starting) and **wiring the onboarding
  activation gate** (`useFirstRunRedirect` — 0 call sites today).

---

## 2. The design-handoff stack (cumulative, latest wins)

**Rule:** the handoffs are sequential and cumulative — **6.12 → 6.24 → 7.2 → 7.21** —
each builds on the prior and **takes precedence where it changes something**; unchanged
foundations carry forward. All four packages live under `Documents/Claude Design/`
(reference only — never imported by the app, per CLAUDE.md §7).

| Handoff | Package | Adds |
| --- | --- | --- |
| **6.12.26** | `…ux_roadmap_and_resources` | UX roadmap + resource/notecard redesign |
| **6.24.26** | `design_handoff_v2_site` | **Baseline v2**: the whole site + design system |
| **7.2.26** | `…Updated Surfaces` (`design_handoff_v2_2026-07-02`) | Day/Week view↔edit modes; lesson editor; lesson popup |
| **7.21.26** | `Design Handoff Update` (2026-07-20) | **Unified Workspace, Shared Composer, pop-in overlay, vibrant recolor** |

### Merged design truth (precedence applied)

The later handoffs are **almost entirely additive on 6.24**. Verified byte-identical
across the stack: the token layer (`colors_and_type.css`), the appearance engine, the
legibility contract, the forking model, and **Setup/`config.jsx`**. Net truth per surface:

- **Site shell** — one site; **six-tab nav: Day · Week · Year · Plan · Post · Teach**;
  navigate-in-place; corner-grammar glass chrome; compact auto-hiding bar on Teach/Plan/Post.
- **Plan tab = Unified Unit/Lesson Workspace** (`ph-workspace`): one modal-with-expand
  planner — 5 tabs (Unit Plan · Lessons · Assessments · Refine · Insights), a
  Units|Lessons left rail, an Overview (progress ring + 6 semantic stat cards + big
  ideas/EQs/vocab/arc), and a right context drawer. It **retires three surfaces**: the
  unit-explorer popup, the focus-lesson popup, and the legacy in-page unit view.
- **Shared Composer** (`ph-composer` / `window.openComposer`) — one app-wide
  add-note/resource surface; a rich note whose each attachment becomes its own stacked
  resource; a shared resource action menu (Open / new tab / copy / edit / remove).
- **Vibrant recolor** — `--uc` (the unit's subject color) organizes the planner; 6
  semantic stat cards. Scoped to the planner; **reuses the locked subject palette** and
  the "themes remap `--accent` only" rule — no global token change.
- **Day/Week view↔edit modes** (`planbook-edit`); **frames = Glass / Bright (the
  near-white paper frame) / Pastel** — the *subject-led* frame is the baseline's
  **Color**, not Bright *(v1.1 correction)*; **unit clicks open the workspace as a
  pop-in overlay** (no navigation); **Post = the Resource Wall**; **new Hero theme**
  (additive).
- **Foundations UNCHANGED** — tokens, the six appearance axes (frame/glass/bg/theme/dim/
  derived-tone), the legibility contract (branch on `data-tone`, never a theme), the
  Team/Personal forking model, "no sharp corners", tokens-only, one-surface-one-job, the
  phone/tablet/desktop + touch contract. Build the design system from `6.24
  design-system/` verbatim; layer only the Hero theme + the planner's `--uc` recolor.

### Vocabulary map — handoff names vs shipped tokens *(v1.1, load-bearing)*

The handoff's frame/theme names do **not** exist as shipped tokens. Building to the
handoff names verbatim hits dead tokens — translate first:

| Handoff says | Shipped reality (`lib/theme-values.ts`) |
| --- | --- |
| Frames "Glass / Bright / Pastel" | `ThemeFrame = "glass" \| "paper" \| "color"` — "Bright" ≈ `paper`; **"bright" in code is a *dim level*, not a frame**; **"Pastel" has no token** (build-or-defer decision in §6) |
| "Hero" theme | **No token.** `AppTheme = clear \| night \| honey \| blossom \| mint \| sky \| off`; "hero" appears only in `app/themes.css` comments |
| Subject-led frame | `color` (the 6.24 baseline's "C/Color" frame) |

Also *(v1.1)*: `lib/palette-data.ts` ships **both** subject→color maps — the shipped v2
map (§4.4) **and** the divergent v1 map as an active remap seam. B-track code must import
the v2 map only.

### Do NOT port (prototype artifacts, per the 7.21 README)
- The **two-iframe `postMessage` bridge** (`cc-scope`/`cc-open-unit`/…). It exists only
  because the prototype iframes the hub. **Production is ONE React tree** — mount the
  workspace as a normal route/modal; every message becomes a direct call/context.
- The **divergent `pw-data.js` dataset** (hub = 4 subjects/11 units vs home = 8
  subjects). A mock artifact — **unify onto one source** (one DB in prod).
- Honest placeholders that are UI-only, not features: the Composer "All-tools" preview
  tiles, the workspace "AI Help" drawer, the Composer "+ Fields". *(v1.1: verified in the
  prototype source itself — `ph-composer.jsx:29` "honest visual placeholders", `:166`
  "+ Fields → coming-soon toast", `ph-workspace.jsx:173` "Coming with the assistant wave".)*

---

## 3. Workspace / account model — the one real reconciliation

The handoffs specify a **simple** model: `profile.role ∈ {team, solo}`, a flat
`workspaces{ active, personal[], teams[] }`, one `team{ name, school, members[] }`, a flat
`curriculums{ active, list[] }`, and (7.21) a per-unit **share link** (a copy-link, not an
ACL). `config.jsx` is byte-identical across the whole stack.

But **richer product decisions were locked (7-17) and are partly shipped**: solo/team
**derived per workspace**, per-course sharing (personal = invisible to team; creator +
admin control), and **full multi-workspace + a v2 workspace-first wizard**. W12b-1 (the
course-sharing RPC seam) and W12c (workspace-first onboarding) already landed.

**Resolution:** the app-model intentionally **exceeds** the handoff's design truth here.
Treat the handoff `config.jsx` as the **visual/section template** for Setup, but wire it
to the **shipped richer model** — not the flat prototype model. This is the crux the
Settings/Config wave must get right (§5.1).

*(v1.1)* **The per-unit share link (and the workspace header's Share ↗ button) is
DEFERRED**, consistent with the user's Wave-9b decision (forgeable base64 token — §6).
It is *not* part of the model to wire; the Share ↗ button ships hidden/disabled until a
signed share-link design exists. This resolves an internal inconsistency (v1.0 §3 implied
wiring it while §6 deferred it).

---

## 4. Contradictions resolved (so no doc conflicts remain)

1. **"master is v1 / rolled back / push freeze"** → FALSE. Master = v2-live (see §1).
2. **Pricing (three stories):** `/welcome` hardcodes "$6/teacher + School tier"; the AI
   plan says "$4.99/$8.99/packs"; CLAUDE.md §1 says packaging is UNDECIDED, don't encode.
   → **CLAUDE.md wins.** Packaging is open. *(v1.1: the `/welcome` cleanup shipped as
   #68 — School tier and $6 removed.)*
3. **Two v2 plans** (the executed 13-wave plan vs the Phase-0–4 `PHASED-PLAN`) → crown the
   **13-wave lineage**. *(v1.1: `PHASED-PLAN.md` is not on master — it exists only in a
   stale working tree, so there is nothing to banner; see §7.)*
4. **Subject→color map** (v1.3-brief vs shipped v2) → the **shipped v2 map is truth**
   (Math→1, UFLI→2, Writing→5, Grammar→7, Spelling→9, Reading→10, SEL→12, Explorers→13).
5. **`/subject`** → already a redirect stub (SubjectView deleted). Drop "retire it" from
   any go-forward list.
6. **Solo/team + workspace model** → reconciled in §3 (shipped richer model, handoff
   `config.jsx` as visual template only).
7. **Onboarding** → `onboarding-v2` shipped but `useFirstRunRedirect` is unwired (blocked
   on a remote onboarding column). This plan owns the activation gate (§5.1).
8. *(v1.1)* **CLAUDE.md §1 was itself stale** — the top authority this plan leans on still
   carried the 2026-05-27 status table ("Phase 1A · mock data drives every view · Supabase
   not wired · 5-tab spine"), contradicting §1 here. → **Resolved by updating CLAUDE.md
   §1's status section to v2-live reality** (same PR as this v1.1). CLAUDE.md remains
   canonical for policy/phasing; its status table now matches this plan.

---

## 5. Go-forward build sequence

Two big open tracks. Recommended order below; each wave ships **behind the flag** and
passes the CLAUDE.md gates (§4a adversarial review, §4b live QA across 6 themes + 3
viewports, §4c data-layer for anything touching the store).

### 5.1 Finish the Settings / Config wave
Remaining after W12a/b-1/c:
- **Full Setup ConfigPage** — build from `6.24 source/config.jsx` as the visual/section
  template (byte-stable across the whole handoff stack → low visual risk), reconciled with
  the **shipped richer workspace model** (§3), not the flat prototype model. Reuse the
  existing settings hooks (`use-account-settings`, `use-subject-settings`,
  `use-schedule-settings`, `use-school-week/-months`, `use-academic-year`, `use-holidays`,
  `use-workspace-settings`, `catchup-state`, `theme`) and the `useSettingsDirty` +
  `SaveConfirmDialog` click-out model — do not rebuild them. Preserve every `/settings/*`
  deep-link and `/settings/team` → **307**.
- **12b-2 multi-workspace** (the real work — data/RLS, not visual). ~~Settings
  sharing-management UI~~ *(v1.1: shipped as #67 U-SHARE.)*
- **Wire the onboarding activation gate** (`useFirstRunRedirect`) — *after* the remote
  onboarding column lands (latent foot-gun until then).

### 5.2 The 7.21 Unified Unit/Lesson Workspace (Track B, B1–B5)
The flagship remaining surface. **A data-layer wave** — units are read-only in the seam
today, so this needs an **additive, nullable migration** (timestamp after
`20260717120000`): editable `units` fields (notes/big_idea/essential_questions/vocab/
standards/…), lesson `taught_at` + rich fields **added to all three fork tables**
(`master_core_lesson_events`, `personal_core_lesson_event_copies`,
`personal_authored_lessons`), and a new `planner_settings` table.
- **B1** shell + Unit Plan · **B2** Lessons editor · **B3** Assessments + Insights +
  **Refine** *(v1.1: previously unowned — 1 of the 5 tabs; it renders the prototype's
  `ph-units.jsx` "Refine table" and is the handoff's least-specified tab: spec it during
  B3 kickoff)* + the right context drawer · **B4** Shared Composer (absorb
  `components/daily/ResourceComposer`; *(v1.1)* also wire the home planbook-edit
  `.pb-rchip` chips to the shared resource menu — the handoff requires one menu across
  surfaces) · **B5** pop-in overlay + retire `UnitDrawer`/focus-lesson path *(v1.1: dropped
  "`SubjectView`" from the retire list — it is already deleted, per §4.5)*.
- *(v1.1)* **B6 — timeline authoring + triage drawer** (NEW, previously uncovered):
  the `ph-units.jsx` timeline interactions (drag ripple/easing, anchor-stacking on drop,
  paint-a-new-unit on empty lanes, zoom slider, hover lesson preview) and the
  under-timeline planning drawer's **Needs Attention** triage (`ph-drawer.jsx` — distinct
  from B3's *right context* drawer; Unit/Lesson Library are plausibly covered by the
  shipped hub-v2 browse layer — verify at build). Sequence after B1–B5 or explicitly
  defer with the user — but it must not stay silently unowned.
- B0 is done; a ready ordered B1 sub-plan (B1.0–B1.8) lives in `agent_shared_log.md`.
- Reuse (don't rewrite): `ExplorerShell`, `UnitExplorer`, `PlanPage` + its tabs,
  `lib/year-v2-data.ts`, `lib/year-unit-aggregate.ts`, the hub-v2 browse/search layer.
  *(v1.1)* **`year-unit-aggregate.ts` has NO unit test** (its sibling `year-v2-data.ts`
  does) — it is the exact module the school-week hazard below points at; add a test
  before B1 builds on it.
- **Porting hazards to avoid** (from B0): model `unit.framework` + the resolution chain
  (never leave `u.fw` unassigned); derive "remaining" from the configured school week
  (Sun–Thu), not a hardcoded /5; write `taught_at` (don't rely on `status` inference);
  `setSaveTarget(id,"core")` is a store **no-op** (never ship a Team-save button that
  claims it reached the team — *(v1.1)* worse, the dead `"core"` path is **type-blessed**:
  `planner-store.tsx` still advertises `target: "personal" | "core"`, so it's callable;
  narrow the type or document why it stays); `Lesson.time` has **no DB column** (a
  time-only write spuriously forks); `ExplorerShell` portals into `.cp-root`, and renaming
  `ue-modal`/`ue-scrim` drops the theme wash.

### 5.3 Verify at build time (couldn't confirm in a read-only pass)
- Is `20260717120000_course_sharing_rpcs.sql` **applied on prod**? (supabase MCP
  `list_migrations`.)
- The live state of the 7-17 workspace-model decisions vs
  `docs/6.6.26 Workspace-Notebook-Team Model.md`.
- *(v1.1)* Did the 7.2 **Day/Week view↔edit toggle** actually ship? (§2 asserts it as
  design truth; §1's shipped list names the `day-v2`/`week-v2`/`lesson-plan-v2` families
  without confirming the toggle itself.)
- *(v1.1)* Does the shipped hub-v2 browse layer satisfy the handoff's **Unit/Lesson
  Library** drawer panes (see B6)?

---

## 6. Non-blocking backlog (carry, don't gate on)
- **Perf:** dual-branch DCE (`sideEffects` + deep imports), standards-catalog → API,
  `next/dynamic` the rich-text editor.
- **Hydration follow-ups:** `/weekly` grid silent surface; skeleton polish; **`/home`
  "Today's lessons" reads a mock helper** (`lib/home/today.ts`) → real fix needed.
- **Post-cutover polish:** Teach board canvas unverified; `/teach` a11y (1.19:1 banner);
  `/catch-up` 3 px overflow @375; `/settings/account` seeded "Lena Haddad".
- **Business-model cleanup:** ~~`/welcome` pricing hardcode + forbidden School tier~~
  *(v1.1: shipped as #68)*; `PROVISIONING_MODE` default (still `"domain"`,
  `lib/supabase/ensure-teacher.ts`); "school admin" → "workspace admin" (live strings in
  `components/standards/StandardsSettings.tsx` + `app/settings/standards/actions.ts`);
  single-source `seat_cap` (default `5` duplicated in `lib/admin/queries.ts` ×2 + the
  `20260606120000` migration).
- **Deferred (explicit):** Wave 9b share-links (forgeable base64 token) — *(v1.1)* this
  **includes the workspace header's Share ↗** (§3); W3 `.vt-scope`.
- *(v1.1)* **Build-or-defer decisions needed (design truth with no build path):** the
  **Pastel frame** (no `ThemeFrame` token — see §2 vocabulary map) and the **compact
  top bar's auto-hide behaviors** (idle hide, tablet peek pill + swipe-down, <640px
  disable — the shipped `ImmersiveBar` has no auto-hide). Decide with the user; record
  the outcome here.
- **AI platform (Phase 3+, far):** gated behind v2 + Supabase-wired; the companion
  curriculum-ingestion plan is missing and would need re-authoring first.

---

## 7. Doc map — what this file replaces

- **Canonical (keep):** `CLAUDE.md`, `BUILD_STANDARD.md`, `AGENTS.md`, this file,
  `agent_shared_log.md` (live session log — never committed),
  `Documents/Project Files/5.16.26 planning_document.md` (master spec/reference).
- **Superseded by this file (bannered on master, kept for detail):**
  `docs/6.24.26-v2-redesign-plan.md`, `docs/7.21.26-v2-cutover-and-workspace-plan.md`
  (its Track B detail is still useful; its Context is stale), `docs/7.16.26-*`,
  `docs/v1.3-redesign-brief.md`.
- *(v1.1 correction)* **Superseded but NOT on master (working-tree-only; nothing to
  banner):** `docs/v2-rebuild/PHASED-PLAN.md`, `docs/v2-rebuild/V2-DELTA-ANALYSIS.md`,
  `docs/v2-rebuild/B1A-NOTES.md` + `B1B-NOTES.md` — these exist only in a stale local
  checkout. v1.0 wrongly listed them as bannered. Treat as transient; the surviving B1
  detail lives in `agent_shared_log.md` (B1.0–B1.8).
- **Reference (keep):** the `6.6.26 Workspace-Notebook-Team *` vocabulary, the historical
  ultraplans/audits under `docs/historical/` + `docs/`, and the operational docs.
- **Transient review artifacts (superseded; safe to remove):** the root
  `CLAUDE-/CODEX-*REVIEW`, `-WAVE1/2-REVIEW`, `-WAVE2-REAUDIT`, `WAVE2-REAUDIT-SYNTHESIS`,
  `QA-REPORT.md`, and the review-prompt scratch files — resolved outputs against the
  now-superseded PHASED-PLAN.
