# Teach Supabase Persistence Port + Go-Live — Ultraplan

> **Status:** EXECUTION ULTRAPLAN — owner directive: *"port the Teach board/widget
> Supabase persistence onto master, then enable Teach-on-Supabase live; use ultraplan
> + dynamic workflow."* Dated 2026-06-07. Branch: `claude/great-noether-zEPd0`,
> fast-forwarded to current `master` (`90f22a3`, PR #7–10 hardening included).
>
> **Builds on** the prior session's investigation (Explore agents mapped the surface;
> reference impl confirmed). **Read with** `CLAUDE.md` (§2 forking, §4a review gate),
> `BUILD_STANDARD.md`. **Gate:** every logic/data change runs the §4a review — on cloud
> that means the mandatory self + independent-agent adversarial review (Codex absent) —
> plus `lint`/`tsc`/`vitest`/`build` green; behavioral proof required before go-live.

---

## 0. Decisions locked
1. **Faithful MANUAL port**, not a cherry-pick. Reference `fe53805` is ~91 commits stale;
   port its logic onto master's current 1182-line file.
2. **Preserve every bit of master's hardening** (§3) — the reference's weaker versions
   must NOT be copied back in.
3. **Go all the way: enable Teach-on-Supabase live** — which makes **behavioral
   verification mandatory** before the prod flag flips.
4. **No new migration** — the columns already exist (`20260531120000_teach_freeform.sql`).
   This is code + a deploy flag only.
5. **Staged, reversible go-live**: merge ships DORMANT (prod flag off) → flip flag →
   live; rollback = unset flag + redeploy.

## 1. Current state (verified 2026-06-07)
- `lib/teach/supabase-source.ts` (master, 1182 lines): stale "SCHEMA GAP" banner +
  ~7 THROWING stubs (`setBoardTags`, `setBoardTheme`, `setBoardRepeat`, `moveWidget`,
  `resizeWidget`, `setWidgetAppearance`, `addPage`) + degraded methods (`listPages`/
  `deletePage`/`reorderPages`, `listBoardsForContext`, `publishBoardToTeamLibrary`,
  `copyTeamBoardToMine`).
- Hardened helpers PRESENT (preserve): `resolveOwnerId` (:432) + `resolveGradeId` (:442)
  THROW on non-UUID; `stripNames()` strips names.
- Columns all present (verified): `boards.{pages,board_theme,repeat,tags,background,
  whiteboard,ephemeral,library_visibility,published_by,source_board_id}`,
  `widgets.{canvas,appearance}`.
- Mock/Supabase switch: `lib/teach/client.ts` reads `NEXT_PUBLIC_TEACH_USE_SUPABASE`
  (unset = mock → Teach is **dormant** in prod today; zero current risk).
- Contract: `TeachDataSource` in `lib/teach/queries.ts`; domain types in `lib/types.ts`.

## 2. Port surface (onto master's file)
1. **Imports** — add `BoardLibraryVisibility`, `CanvasPosition`, `RepeatRule`.
2. **Row types** — add new columns to `BoardRow` / `WidgetRow`.
3. **Column lists** — add them to `BOARD_COLS` / `WIDGET_COLS`.
4. **Mappers** — extend `rowToBoard` / `rowToWidget` / `widgetToRow` / `boardPatchToRow`
   / `widgetPatchToRow` (incl. pages materialization).
5. **Helpers** — `pagesOf`, `commitPages` (delete-then-insert page-0 mirror), `findWidget`,
   `clampWidth` (230–640), `newPageId`, `persistWidgetPatch`, `copyBoardContent`.
6. **KEPT filter** — `ephemeral=false AND library_visibility≠'team'` in `myBoardRows` /
   `assertUnderCap`.
7. **Implement** the throwing/degraded methods per the reference.

## 3. CRITICAL — do NOT regress master's hardening
- **KEEP `resolveOwnerId` / `resolveGradeId` that THROW** on non-UUID (:432–452). The
  reference's pass-through versions must NOT be copied.
- **KEEP `stripNames()`** name-stripping everywhere it's applied (incl. in
  `upsertWidgetOnPage`).
- In `publishBoardToTeamLibrary`, add **`assertUuid(resolveOwnerId(ownerId))`** before the
  `.or()` interpolation (filter-injection defense).
- **Keep the AUTHED server client** (cookie session), never service-role — RLS must apply.
- Net: the port adds persistence; it must be a strict superset of master's safety.

## 4. Verification (behavioral proof is the go-live gate)
- **Static/build:** `npx tsc --noEmit`, `npm run lint`, `npm run build` green.
- **Unit tests** (`vitest`) for the PURE helpers/mappers with fixtures: `clampWidth`,
  `pagesOf`, `rowToBoard`/`rowToWidget`, patch mappers. (Pure functions → no DB needed.)
- **Read-only DB confirm:** the 5.31 columns exist live (no migration needed).
- **Behavioral E2E behind the flag, BEFORE go-live** (the non-negotiable proof):
  `NEXT_PUBLIC_TEACH_USE_SUPABASE=1` in `.env.local` → `npm run dev` → sign in (Claude
  bypass) → `/teach` → create board + widgets → move/resize/theme/add-page → reload →
  confirm persistence via `supabase db query --linked` → DELETE the test board → revert
  `.env.local`. *(Requires Supabase access + the bypass token in the run env; if absent
  here, this is a user-run pre-go-live step — see §6 gate.)*

## 5. Audits (per MD §4a — both, this is data-handling)
- **Internal** (orchestrator): review the full diff; confirm every hardening item in §3
  is preserved; check mappers round-trip + null/undefined handling.
- **External** (independent agent that did NOT write the port): adversarial review +,
  where feasible, behavioral/contract reproduction. Hunt for: dropped hardening,
  mapper data-loss, page-0 mirror corruption, cap-filter logic errors, RLS bypass,
  injection in `.or()` interpolations.
- Resolve every Critical/High before merge; re-run until `NO BLOCKING ISSUES`.

## 6. Go-live (staged, reversible) — USER-GATED steps flagged
1. **Merge** `great-noether` → `master` → deploys **DORMANT** (prod flag off). *(needs
   user authorization — never push master without it.)*
2. **Behavioral E2E** must be green first (§4).
3. **Flip live:** set GitHub Actions secret `NEXT_PUBLIC_TEACH_USE_SUPABASE=1` + re-run
   "Deploy to Cloudflare". *(needs prod access.)*
4. **Verify in prod**, then **ROLLBACK path** = unset the flag + redeploy → back to mock.
- Blast radius: beta has 1 provisioned teacher; small, but persistence correctness matters.

## 7. Dynamic workflow (agents + ownership)
- **W-PORT (agent):** the faithful manual port of `lib/teach/supabase-source.ts` —
  reads `fe53805` + master, ports the surface (§2), preserves all §3 hardening, returns
  a method-by-method diff summary + how each hardening item was kept. Owns ONLY that file.
- **W-TEST (agent):** unit tests for the pure helpers/mappers (§4). Owns ONLY a new
  `tests/teach-*.test.ts`. Runs after W-PORT lands the helper signatures.
- **Orchestrator (me):** integrate, run `tsc`/`lint`/`vitest`/`build`, the internal audit,
  spawn the **external audit** agent, drive findings to `NO BLOCKING ISSUES`, then gate
  go-live to the user. No agent commits; I integrate + commit each reviewed unit.

## 8. Risks
| Risk | Impact | Mitigation |
|---|---|---|
| Port silently drops master's hardening | High | §3 checklist enforced in port brief + both audits explicitly verify each item |
| Mapper data-loss / page-0 mirror corruption | High | Unit tests with fixtures; external audit round-trips mappers |
| Enabling live on unverified persistence | High | Behavioral E2E is a hard gate before the flag flips (§4/§6) |
| `.or()` filter injection in publish | Med | `assertUuid` guard (§3); audit checks all interpolations |
| Stale-reference drift mis-port | Med | Manual port + method-by-method diff review, not cherry-pick |
| Cross-session branch drift | Low | Already resolved — FF'd to current master; no new migration |
