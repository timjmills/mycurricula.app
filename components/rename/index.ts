// components/rename/ — per-instance renaming affordance.
//
// Pairs with lib/instance-labels.tsx (the store). Drop <InstanceRenameLabel>
// anywhere an instance name (Subject / Unit / Week / Lesson) is shown to make
// it renameable inline, with a per-rename "Just me / Whole team" scope choice.

export { InstanceRenameLabel } from "./InstanceRename";
export type { InstanceRenameLabelProps } from "./InstanceRename";
