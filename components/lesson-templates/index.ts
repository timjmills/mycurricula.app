// Barrel — public surface of @/components/lesson-templates.
//
// LessonTemplatesManager  — the full settings page component (owns layout,
//                           built-in gallery, custom template list, editor).
// TemplateSectionEditor   — controlled editor for a template's section list
//                           (created by sibling agent in the same build run).

export { LessonTemplatesManager } from "./lesson-templates-manager";
export { TemplateSectionEditor } from "./template-section-editor";
