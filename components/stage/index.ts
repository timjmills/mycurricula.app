// Public surface of the stage family — the app-wide background engine's
// client leaves. The stage itself is a plain server-rendered <div class="stage">
// in app/layout.tsx; this folder holds the small client components that drive
// its behaviour from the root layout.
export { DenseRouteFlag } from "./DenseRouteFlag";
