// components/team/ — public barrel (CLAUDE.md §3 naming / barrel convention).
//
// Consumers import from the folder (`@/components/team`), never a deep file.

export { TeamPage } from "./TeamPage";
export type { TeamPageProps } from "./TeamPage";
