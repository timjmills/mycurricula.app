// components/teach/tools/ — the "Dockable Tools" side panel. Teacher tool-widgets
// (timer, dice, poll, …) docked as a vertical, internally-scrolling stack.
// Consumers import from this barrel, never a deep file.

export { ToolsDock } from "./ToolsDock";
export type { ToolsDockProps, DockedTool } from "./ToolsDock";
